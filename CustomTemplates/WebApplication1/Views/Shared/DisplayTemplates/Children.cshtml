@model List<WebApplication1.Models.Child>

@if (Model.Count() == 0)
{
    <p>No children</p>
}
else
{
    for (int i = 0; i < Model.Count(); i++)
    {
        @Html.DisplayFor(model => model[i])
    }
}
