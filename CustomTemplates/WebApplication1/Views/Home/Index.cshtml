@using WebApplication1.Models
@model List<Product>


<table>
    @for(int i =0; i<Model.Count(); i++)
    {
        <p>
            @Html.DisplayFor(m=>m[i])
        </p>
    }
</table>

@Html.ActionLink("Add", "Add")
